"""MCP (Model Context Protocol) guidance server for pyTOST.

Exposes onboarding/guidance content (overview, engine-selection guide, API
pointers) to MCP-capable agents/editors over stdio, so a fresh agent can
discover when/how to use pyTOST without reading the source. See
``pyTOST.mcp.server`` and the project README.
"""
